# BPTT Rollout Transition

- [x] Add `sequence_length: 50` to `config/default.yaml`
- [x] Migrate `src/datasets.py` to chunk `x_t` vectors cleanly into `[seq_len, 2]` arrays
- [x] Rewrite all models in `src/models.py` to include an autoregressive `for` loop that recursively rolls out `x_t` natively
- [x] Update `evaluate.py` to seamlessly flatten and calculate the global trajectory sequences against the ground truth
- [x] Ensure shapes array accurately in `src/train.py`, test the execution to confirm the loss backward-flows efficiently over all iterations
