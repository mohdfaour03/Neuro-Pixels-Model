# Phase 2: E/I Latent Structure and Dynamical Evaluation

- [ ] Update `config/default.yaml` with Phase 2 configurations
- [ ] Add explicit `LinearBaseline` to `models.py`
- [ ] Refactor `MechanisticModel` and `HybridModel` to return `(x, E, I)` tuples
- [ ] Add configurable activation functions and observation models
- [ ] Update `train.py` to handle tuple outputs and optional regularization
- [ ] Update `evaluate.py` with temporal, derivative, and stability metrics
- [ ] Implement new plots in `visualize.py` (Phase plot, overlay, residual)
- [ ] Connect and run Phase 2 pipeline in `run_experiment.py`
