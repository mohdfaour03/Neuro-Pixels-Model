# Autoregressive Validation & Residual Penalization

- [ ] Re-write `evaluate_model` inside `src/evaluate.py` to recursively generate a continuous prediction from exactly $x_0$ down the entire sequence without ground-truth snapping.
- [ ] Implement an $L2$ constraint calculation onto the `residual_net` outputs inside `HybridModel.forward()` natively and return it to the optimizer loop!
- [ ] Add the `residual_loss` scale multiplier into `src/train.py` during `loss.backward()`.
- [ ] Run `--mode lightweight` to visually confirm seamless limit-cycle plotting boundaries!
