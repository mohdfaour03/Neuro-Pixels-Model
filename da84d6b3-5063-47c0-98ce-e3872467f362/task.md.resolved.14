# Phase 2: E/I Latent Structure and Dynamical Evaluation

- [x] Update `config/default.yaml` with Phase 2 configurations
- [x] Add explicit `LinearBaseline` to `models.py`
- [x] Refactor `MechanisticModel` and `HybridModel` to return `(x, E, I)` tuples
- [x] Add configurable activation functions and observation models
- [x] Update `train.py` to handle tuple outputs and optional regularization
- [x] Update `evaluate.py` with temporal, derivative, and stability metrics
- [x] Implement new plots in `visualize.py` (Phase plot, overlay, residual)
- [x] Connect and run Phase 2 pipeline in `run_experiment.py`
